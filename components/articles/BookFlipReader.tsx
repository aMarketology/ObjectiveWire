'use client';

/**
 * BookFlipReader
 * Adds page-turn proximity scroll-snap behavior triggered when the reader
 * reaches an H2 heading inside the article body ([data-article-body="true"]).
 *
 * Renders nothing visible — purely a behavioral enhancement.
 */
export default function BookFlipReader() {
  return null;
}
